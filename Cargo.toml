[package]
name = "ds4-bt-usb-probe"
version = "0.3.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Remote-testable UHID probe for DS4/PS4-compatible controller identity under Proton"

[dependencies]
libc = "0.2"
